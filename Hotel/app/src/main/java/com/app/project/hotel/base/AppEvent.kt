package com.app.project.hotel.base

enum class AppEvent {
     OTHER_ERROR,
     USER_EXISTS,
     SUCCESS,
     USER_NOT_EXISTS,
     PASSWORD_ERROR,
     USER_PASS_EMPTY,
     NETWORK_ERROR,
     NO_ROOMS
}